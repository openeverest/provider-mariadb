apiVersion: v1
kind: Service
metadata:
  name: {{ include "provider-mariadb.fullname" . }}
  labels:
    {{- include "provider-mariadb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "provider-mariadb.selectorLabels" . | nindent 4 }}
