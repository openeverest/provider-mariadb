# The provider seeds the restored Instance by wiring MariaDB.spec.bootstrapFrom
# to the physical backup content type.
apiVersion: k8s.mariadb.com/v1alpha1
kind: MariaDB
metadata:
  name: physical-restored
spec:
  bootstrapFrom:
    backupContentType: Physical
